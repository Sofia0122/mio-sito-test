# Workflow Di Sviluppo

Comandi:

```bash
npm run dev
npm run build
npm run lint
```

Ogni step implementativo deve mantenere `npm run build` funzionante. Aggiungi codice feature seguendo il flusso page -> component -> hook -> service -> repository.
