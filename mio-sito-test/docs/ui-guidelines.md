# UI Guidelines

Harbourly is mobile-first for customer flows and desktop-optimized for admin operations.

Use Tailwind CSS, reusable generic UI components and accessible labels, focus states and keyboard-friendly controls. Avoid business-specific code in `components/ui`.

When Figma MCP is provided, use Figma as the source of truth for tokens, layout, typography and responsive behavior.
