# Development Workflow

Run:

```bash
npm run dev
npm run build
npm run lint
```

Every implementation step should keep `npm run build` passing. Add feature code through the page -> component -> hook -> service -> repository flow.
