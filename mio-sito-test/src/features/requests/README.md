# requests

Owns service request creation, listing, detail and tracking behavior.

Types live in `types/` and are shared by mock and HTTP repositories. Mock data must stay in `src/mocks`; repository implementations adapt it through the common contract.
