# features

Feature modules own business-specific components, hooks, services, schemas, types and repository contracts.

Each data feature follows: component -> hook -> service -> repository interface -> mock or HTTP repository.
