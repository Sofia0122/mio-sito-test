# src

Contains the Harbourly frontend source code. Keep route bootstrapping in `app`, thin pages in `pages`, business modules in `features`, shared primitives in `components`, infrastructure in `lib`, temporary data in `mocks`, global styles in `styles` and cross-feature types in `types`.

Do not place backend code, secrets or direct fetch calls in pages or UI components.
