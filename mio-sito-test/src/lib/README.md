# lib

Shared frontend infrastructure: API client, query client, repository factory, constants, date and formatting helpers.

Direct data-source selection must stay centralized here rather than being spread through features or pages.
